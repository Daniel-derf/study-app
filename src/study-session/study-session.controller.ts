import {
  Controller,
  // Get,
  // Post,
  // Body,
  // Patch,
  // Param,
  // Delete,
} from '@nestjs/common';
// import { StudySessionService } from './study-session.service';
// import { CreateStudySessionDto } from './dto/create-study-session.dto';
// import { UpdateStudySessionDto } from './dto/update-study-session.dto';

@Controller('study-session')
export class StudySessionController {
  // constructor(private readonly studySessionService: StudySessionService) {}
  // @Post()
  // create(@Body() createStudySessionDto: CreateStudySessionDto) {
  //   return this.studySessionService.create(createStudySessionDto);
  // }
  // @Get()
  // findAll() {
  //   return this.studySessionService.findAll();
  // }
  // @Get(':id')
  // findOne(@Param('id') id: string) {
  //   return this.studySessionService.findOne(+id);
  // }
  // @Patch(':id')
  // update(
  //   @Param('id') id: string,
  //   @Body() updateStudySessionDto: UpdateStudySessionDto,
  // ) {
  //   return this.studySessionService.update(+id, updateStudySessionDto);
  // }
  // @Delete(':id')
  // remove(@Param('id') id: string) {
  //   return this.studySessionService.remove(+id);
  // }
}
